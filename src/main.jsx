import React from 'react';
import ReactDOM from 'react-dom/client';
import ScheduleSimulator from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ScheduleSimulator />
  </React.StrictMode>
);
